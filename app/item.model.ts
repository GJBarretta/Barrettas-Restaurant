export class Item {
    public name: string;
    public desc: string;
    public price: string;

    constructor(name: string, desc: string, price: string){
        this.name = name;
        this.desc = desc;
        this.price = price;
    }
}